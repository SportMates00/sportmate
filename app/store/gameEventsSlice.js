import { createSlice } from '@reduxjs/toolkit';


const gameEventsSlice = createSlice({
  name: 'GameEvents',
  initialState:  [] ,
  reducers: {
    
}});

export const {  } = gameEventsSlice.actions;

export default gameEventsSlice.reducer;